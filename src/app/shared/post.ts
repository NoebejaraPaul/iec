export interface Post {
    key?: string | null,
    ID: string | null,
    Name: string | null,
    Party: string | null,
    Password: string | null,
    Phone: string | null,
    Vode: string | null,
}
